# Binaries — Makefile builds into bin/ (`go build -o bin/nextcloud-pp-cli`).
# Anchor with bin/ so the cmd/nextcloud-pp-cli/ source directory isn't
# accidentally ignored by the unanchored binary name.
bin/
build/

# Go
*.test
*.out
vendor/

# OS
.DS_Store
Thumbs.db
